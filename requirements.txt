fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
soundfile>=0.12.1     # For WAV encoding
numpy>=1.26.0
python-multipart>=0.0.9 # For form data if needed (though we use JSON)
# Kokoro & ML Deps (Matches kokoro_tts/pyproject.toml)
torch>=2.2.0          # CPU version by default, or specific CUDA index url
transformers>=4.40.0
huggingface_hub>=0.22.0
scipy>=1.13.0         # Often needed for audio signal processing
misaki[en]>=0.9.4     # G2P requirement
kokoro>=0.3.4         # Main TTS library
